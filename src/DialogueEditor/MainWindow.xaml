<Window x:Class="DialogueEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DialogueEditor.ValidationRules"
        xmlns:bll="clr-namespace:DialogueEditor.BLL;assembly=DialogueEditor.BLL" 
        d:DataContext="{d:DesignInstance Type=bll:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel >
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Command="New" Content="New" Click="Button_Click" />
                <Button Content="Open" Click="Button_Click_Open_File" />
                <Button Content="Save" />
            </ToolBar>
        </ToolBarTray>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!--Отображение ключей словаря-->
            <ListBox x:Name="KeysListBox" ItemsSource="{Binding TagSteps.Keys }" SelectedItem="{Binding SelectedTag}"/>

            <!--Отображение элементов типа Step-->
            <ListBox Grid.Column="1" ItemsSource="{Binding Steps, Mode=TwoWay}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="Текст Детектива"/>
                            <TextBox Text="{Binding Question}" FontWeight="Bold"/>

                            <TextBlock Text="Текст Доктора"/>
                            <TextBox Text="{Binding Answer}" Margin="0,4,0,0"/>
                            
                            <ListBox ItemsSource="{Binding Variants, Mode=TwoWay}" Margin="0,5,0,0">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="20,10,0,0">
                                            <TextBox Text="{Binding Question}" FontStyle="Italic"/>
                                            <TextBox Text="{Binding Answer}" Margin="0,4,0,0"/>
                                            <TextBox Margin="0,4,0,0" Foreground="Blue"
                                               TextDecorations="Underline" Cursor="Hand">
                                                <TextBox.Text>
                                                    <Binding Path="LinkTeg"
                                                        UpdateSourceTrigger="PropertyChanged" >
                                                        <Binding.ValidationRules>
                                                            <local:TagStepValidator />
                                                        </Binding.ValidationRules>
                                                    </Binding>
                                                </TextBox.Text>
                                            </TextBox>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </DockPanel>

</Window>
